"use strict" // treat all the js code as newer version of javascript.


// alert("8+5");   -> we are using nodeJs not browser to run this.

let name ="heuffhf";
let age =4
let isloggedIn =false;

// number => 2 to the power 53
// bigint
// string => ""
// boolean => true/false
// null => standaloneValue => used as typeof then -> object.
// undefined => nothing is assigned -> when used with typeof => undefined
// symbol => unnique then symbol

// object

console.log(typeof age);